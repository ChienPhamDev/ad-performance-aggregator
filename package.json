{
  "name": "ad-performance-aggregator",
  "version": "1.0.0",
  "main": "aggregator.js",
  "scripts": {
    "unzip": "tar -xf ad_data.csv.zip -C output",
    "single": "node aggregator.js --input ad_data.csv --output results/",
    "parallel": "node aggregator.js --input ad_data.csv --output results/ --parallel"
  },
  "keywords": ["csv", "data-processing", "cli"],
  "author": "",
  "license": "ISC",
  "description": "Ad Performance Aggregator - CSV processing CLI tool",
  "dependencies": {
    "cli-progress": "^3.12.0",
    "commander": "^14.0.3"
  }
}
